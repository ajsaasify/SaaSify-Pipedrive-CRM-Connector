export enum PrivateOffersView {
  DETAILS = "details",
  SUBSCRIPTION = "subscription",
  USAGE = "usage",
  ACTIVITY = "activity",
}

export enum DefaultView {
  COSELL = "cosell",
  PRIVATE_OFFER = "privateOffers",
  DASHBOARD = "dashboard",
}
